include:
  - templates.yaml

App\Entityh\User:
  user{1..5} (extends user):
  admin (extends user):
    username: linkmat
    email: admin@doe.fr
    roles: ["CMS_MANAGE"]

App\Entity\Posts:
  post{1..5} (extends post):

App\Entity\Category:
  category{1..5} (extends category):