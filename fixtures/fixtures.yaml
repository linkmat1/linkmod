include:
  - templates.yaml

App\Domain\Auth\User:
  user{1..5} (extends user):
  admin (extends user):
    username: linkmat
    email: admin@doe.fr


App\Domain\Blog\Post:
  post{1..50} (extends post):
    category: '@category*'

