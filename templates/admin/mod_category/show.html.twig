{% extends 'base.html.twig' %}

{% block title %}ModCategory{% endblock %}

{% block body %}
    <h1>ModCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mod_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ mod_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mod_category_index') }}">back to list</a>

    <a href="{{ path('mod_category_edit', {'id': mod_category.id}) }}">edit</a>

    {{ include('mod_category/_delete_form.html.twig') }}
{% endblock %}
