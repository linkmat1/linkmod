{% extends 'base.html.twig' %}

{% block title %}ForumCategory{% endblock %}

{% block body %}
    <h1>ForumCategory</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ forum_category.id }}</td>
        </tr>
        <tr>
            <th>Title</th>
            <td>{{ forum_category.title }}</td>
        </tr>
        <tr>
            <th>Position</th>
            <td>{{ forum_category.position }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('forums_forum_category_index') }}">back to list</a>

    <a href="{{ path('forums_forum_category_edit', {'id': forum_category.id}) }}">edit</a>

    {{ include('admin/forums/forum_category/_delete_form.html.twig') }}
{% endblock %}
