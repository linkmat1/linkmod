{% extends 'base.html.twig' %}

{% block title %}Linkmod{% endblock %}

{% block body %}
<div class="container">
    <article class="home__mods" >
        {%  for row in rows %}
            <div class="card">
                <div class="card__title"><h2>{{ row.title }}</h2></div>
                <div class="card__content">
                    {{ row.content }}
                </div>
            <div class="card__meta">
            <time-ago time="{{ row.createdAt | date }}"></time-ago>
        </div>
            </div>
        {%  endfor %}
    </article>
</div>


{% endblock %}
